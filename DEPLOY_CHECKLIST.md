# 部署檢查清單 ✅

## 📋 部署前檢查

### ✅ 本地測試
- [x] 資料庫表已創建 (`course_messages`)
- [x] 測試腳本執行通過
- [x] 構建成功 (`npm run build`)
- [x] 所有 TypeScript 錯誤已修復
- [x] API 檔案在正確位置

### 📁 檔案確認

**新增的 API 端點：**
- [x] `functions/api/v1/courses/[courseId]/students.ts`
- [x] `functions/api/v1/courses/[courseId]/messages.ts`

**新增的前端頁面：**
- [x] `src/views/instructor/CourseStudentsView.vue`
- [x] `src/views/courses/CourseMessagesView.vue`

**修改的檔案：**
- [x] `src/router/index.ts` (新增路由)
- [x] `src/views/instructor/InstructorMyCoursesView.vue` (新增學員管理按鈕)
- [x] `src/views/courses/CourseDetailView.vue` (新增查看訊息按鈕)

## 🚀 部署步驟

### 方法 1: 使用 Git 部署（推薦）

```bash
# 1. 提交所有變更
git add .
git commit -m "feat: 新增講師學員管理和訊息功能"

# 2. 推送到遠端
git push origin main
```

Cloudflare Pages 會自動偵測到變更並開始部署。

### 方法 2: 使用 Wrangler CLI

```bash
# 1. 安裝 Wrangler（如果還沒安裝）
npm install -g wrangler

# 2. 登入 Cloudflare
wrangler login

# 3. 部署
wrangler pages deploy dist
```

## 🔍 部署後驗證

### 1. 檢查 API 端點

**測試學員名單 API：**
```bash
# 替換 YOUR_DOMAIN 和 YOUR_INSTRUCTOR_TOKEN
curl -X GET https://YOUR_DOMAIN.pages.dev/api/v1/courses/1/students \
  -H "Authorization: Bearer YOUR_INSTRUCTOR_TOKEN"
```

預期回應：
```json
{
  "success": true,
  "data": {
    "course": {...},
    "students": [...],
    "total": 1
  }
}
```

**測試訊息 API：**
```bash
curl -X GET https://YOUR_DOMAIN.pages.dev/api/v1/courses/1/messages \
  -H "Authorization: Bearer YOUR_STUDENT_TOKEN"
```

### 2. 檢查前端頁面

訪問以下 URL 確認頁面可以正常載入：

- [ ] `https://YOUR_DOMAIN.pages.dev/instructor/my-courses`
- [ ] `https://YOUR_DOMAIN.pages.dev/instructor/courses/1/students`
- [ ] `https://YOUR_DOMAIN.pages.dev/courses/1/messages`

### 3. 功能測試

#### 講師功能測試
1. [ ] 使用講師帳號登入
2. [ ] 進入「我的授課」頁面
3. [ ] 點擊「學員管理」按鈕
4. [ ] 確認可以看到學員名單
5. [ ] 點擊「發送訊息給學員」
6. [ ] 填寫並發送群發訊息
7. [ ] 確認訊息發送成功

#### 學員功能測試
1. [ ] 使用學員帳號登入
2. [ ] 進入已報名的課程詳情頁
3. [ ] 點擊「查看課程訊息」
4. [ ] 確認可以看到講師發送的訊息

#### 權限測試
1. [ ] 講師無法看到「立即報名」按鈕
2. [ ] 學員無法訪問其他課程的訊息
3. [ ] 講師無法訪問其他講師的學員名單

## 🐛 常見問題排查

### 問題 1: API 返回 404
**可能原因：**
- Functions 檔案路徑不正確
- Cloudflare Pages 尚未完成部署

**解決方案：**
1. 檢查 `functions/api/v1/courses/[courseId]/` 目錄是否存在
2. 等待 Cloudflare Pages 部署完成（通常 2-5 分鐘）
3. 檢查 Cloudflare Pages 控制台的部署日誌

### 問題 2: API 返回 500
**可能原因：**
- 資料庫連接問題
- 環境變數未設置

**解決方案：**
1. 在 Cloudflare Pages 控制台檢查環境變數
2. 確認 `DATABASE_URL` 已正確設置
3. 檢查 Cloudflare Pages 的 Functions 日誌

### 問題 3: 前端頁面空白
**可能原因：**
- 路由配置問題
- JavaScript 錯誤

**解決方案：**
1. 打開瀏覽器開發者工具查看錯誤
2. 檢查 Console 是否有 JavaScript 錯誤
3. 檢查 Network 標籤是否有 API 請求失敗

### 問題 4: 權限錯誤
**可能原因：**
- Token 無效或過期
- 用戶角色不正確

**解決方案：**
1. 重新登入獲取新的 token
2. 檢查用戶的 `userType` 是否正確
3. 檢查瀏覽器的 localStorage 和 sessionStorage

## 📊 部署狀態檢查

### Cloudflare Pages 控制台

1. 登入 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 選擇你的專案
3. 查看「Deployments」標籤
4. 確認最新的部署狀態為「Success」

### 部署日誌

檢查以下內容：
- [ ] 構建成功
- [ ] Functions 已部署
- [ ] 沒有錯誤訊息

## 🎯 部署完成確認

全部完成後，確認以下項目：

- [ ] Git 已推送到遠端
- [ ] Cloudflare Pages 部署成功
- [ ] API 端點回應正常
- [ ] 前端頁面可以訪問
- [ ] 講師可以查看學員名單
- [ ] 講師可以發送訊息
- [ ] 學員可以查看訊息
- [ ] 權限控制正常運作

## 📝 部署記錄

**部署時間：** _____________

**部署版本：** _____________

**部署者：** _____________

**測試結果：**
- 學員名單 API: ⬜ 通過 ⬜ 失敗
- 訊息 API: ⬜ 通過 ⬜ 失敗
- 前端頁面: ⬜ 通過 ⬜ 失敗
- 權限控制: ⬜ 通過 ⬜ 失敗

**備註：**
_____________________________________________
_____________________________________________

## 🔄 回滾計劃

如果部署後發現問題，可以：

1. **在 Cloudflare Pages 控制台回滾：**
   - 進入「Deployments」標籤
   - 找到上一個穩定版本
   - 點擊「Rollback to this deployment」

2. **使用 Git 回滾：**
   ```bash
   git revert HEAD
   git push origin main
   ```

## 📞 支援資源

- **文檔：** `INSTRUCTOR_FEATURES.md`
- **部署指南：** `DEPLOYMENT_GUIDE.md`
- **快速開始：** `QUICK_START.md`
- **實現總結：** `IMPLEMENTATION_SUMMARY.md`

---

✅ 所有檢查項目完成後，系統即可投入使用！
